import { ColorEstadosDirective } from './color-estados.directive';

describe('ColorEstadosDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorEstadosDirective();
    expect(directive).toBeTruthy();
  });
});
