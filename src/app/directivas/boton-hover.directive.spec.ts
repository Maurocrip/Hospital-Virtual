import { BotonHoverDirective } from './boton-hover.directive';

describe('BotonHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonHoverDirective();
    expect(directive).toBeTruthy();
  });
});
