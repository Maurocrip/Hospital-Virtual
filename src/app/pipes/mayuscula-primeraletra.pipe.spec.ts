import { MayusculaPrimeraletraPipe } from './mayuscula-primeraletra.pipe';

describe('MayusculaPrimeraletraPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculaPrimeraletraPipe();
    expect(pipe).toBeTruthy();
  });
});
